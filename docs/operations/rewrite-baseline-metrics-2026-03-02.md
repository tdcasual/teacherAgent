# Rewrite Baseline Metrics (2026-03-02)

- Baseline commit: `b3809e2`
- Baseline business LOC snapshot command:
  `rg --glob '!frontend/node_modules/**' --glob '!frontend/dist/**' --glob '*.go' --glob '*.ts' --glob '*.tsx' '^' services frontend`
- Baseline LOC result: `86966`
- Raw snapshot file path (local, ignored): `/tmp/rewrite_loc_snapshot.txt`

Notes:

- `output/` is gitignored in this repository, so baseline values are recorded in this tracked doc for acceptance comparison.
